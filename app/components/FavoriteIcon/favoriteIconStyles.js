import { css } from 'styled-components';

const favoriteIconStyles = css`
  color: #e0b73a;
  width: 24px;
  height: 24px;

  &:hover {
    cursor: pointer;
  }
`;

export default favoriteIconStyles;
