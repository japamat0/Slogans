/*
 * PageNumber Messages
 *
 * This contains all the text for the PageNumber component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PageNumber';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PageNumber component!',
  },
});
